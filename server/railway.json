{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "buildCommand": "pwd && cd ../client && npm install && npm run build && cd ../server && npm install"
    },
    "deploy": {
        "startCommand": "npm start"
    }
}